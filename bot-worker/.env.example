# Required
#
# PORT:
# - Local listen port for this worker instance.
# - Pick an unused port per replica on the same host.
PORT=3000
#
# GITHUB_WEBHOOK_SECRET:
# - GitHub App webhook secret from GitHub App settings.
# - GitHub: Settings -> Developer settings -> GitHub Apps -> <Your App> -> Webhook -> Secret.
GITHUB_WEBHOOK_SECRET=replace_with_github_webhook_secret
#
# GITHUB_APP_ID:
# - Numeric GitHub App ID from GitHub App settings.
# - GitHub: Settings -> Developer settings -> GitHub Apps -> <Your App> -> App ID.
GITHUB_APP_ID=123456
#
# GITHUB_APP_PRIVATE_KEY:
# - PEM private key generated in GitHub App settings.
# - GitHub: Settings -> Developer settings -> GitHub Apps -> <Your App> -> Private keys -> Generate a private key.
# - Keep quoted with \n escapes if stored in one line.
GITHUB_APP_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----\n"
#
# BACKEND_BASE_URL:
# - Public base URL of backend-api that serves /internal/v2/* endpoints.
BACKEND_BASE_URL=https://sitg.io
#
# BACKEND_BOT_KEY_ID:
# - Service bot key ID provisioned by backend-api/SITG internal auth.
# - Use the key id issued for centralized bot-worker auth.
BACKEND_BOT_KEY_ID=bck_live_replace_me
#
# BACKEND_INTERNAL_HMAC_SECRET:
# - Secret paired with BACKEND_BOT_KEY_ID for x-sitg-signature HMAC.
# - Issued when creating/rotating the bot service key in backend-api.
BACKEND_INTERNAL_HMAC_SECRET=replace_with_bot_service_hmac_secret

# Recommended
#
# WORKER_ID:
# - Stable unique worker identity in logs and claim/result messages.
WORKER_ID=bot-worker-prod-1
#
# OUTBOX_POLLING_ENABLED:
# - Keep true in normal operation.
OUTBOX_POLLING_ENABLED=true
#
# OUTBOX_POLL_INTERVAL_MS:
# - Poll interval for outbox claim loop.
OUTBOX_POLL_INTERVAL_MS=5000
#
# OUTBOX_CLAIM_LIMIT:
# - Max actions claimed per poll.
OUTBOX_CLAIM_LIMIT=25

# Optional
#
# GITHUB_API_BASE_URL:
# - Override only for GitHub Enterprise or local mocks.
GITHUB_API_BASE_URL=https://api.github.com
#
# BACKEND_SERVICE_TOKEN:
# - Optional bearer token if backend is configured to require it in addition to HMAC.
BACKEND_SERVICE_TOKEN=
